//Modules
const Ngcobo = 'Ngcobo';
const Mthatha = 'Mthatha';

const sayHi = (name) =>
{
    console.log(`Hello there ${name}`);
}

// Exporting the function
sayHi ('Slindo');
sayHi (Ngcobo);
sayHi (Mthatha);